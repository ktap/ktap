#!/usr/bin/env ktap

# get output of this script by below command:
# cat /sys/kernel/debug/tracing/trace_pipe

trace "syscalls:*" function (e) {
	trace_printk("%d %d\t%s\t%s", cpu(), pid(), execname(), e.tostring())
}

