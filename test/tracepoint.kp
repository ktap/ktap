#!/usr/bin/env ktap

local function failed() {
	printf("failed\n");
	exit(-1);
}

#----------------------------------------#

local n = 0

trace sched:* {
	n = n + 1
}

tick-1s {
	if (n == 0) {
		failed()
	}
	exit()
}

