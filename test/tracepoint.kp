#!/usr/bin/env ktap

function failed() {
	printf("failed\n");
	exit(-1);
}

#----------------------------------------#

count = 0

trace "sched:*" function (e) {
	count = count + 1
}

os.sleep(10)

kdebug.traceoff()

if (!count) {
	failed()
}

