#!/usr/bin/env ktap

function failed() {
	printf("failed\n");
	exit(-1);
}

#----------------------------------------#

count = 0
function eventfun(e) {
	count = count + 1
}

kdebug.probe("tp:sched", eventfun)

os.sleep(10)

kdebug.traceoff()

if (!count) {
	failed()
}

