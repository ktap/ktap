#!/usr/bin/env ktap

function failed() {
	printf("failed\n");
	exit(-1);
}

#-----------------------------------------#

sched_clock = `sched_clock`
printf("sched_clock address: 0x%x\n", sched_clock);

sched_clock_data = `sched_clock_data`
printf("sched_clock_data address: 0x%x\n", sched_clock);

if (sched_clock == sched_clock_data) {
	failed()
}
