#!/usr/bin/env ktap

local function failed() {
	printf("failed\n");
	exit(-1);
}

#---------------------------------------#

local t = {}

t["key"] += 1
if (t["key"] != 1) {
	failed()
}

t["key"] += 1
if (t["key"] != 2) {
	failed()
}
