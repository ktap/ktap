
function failed() {
	printf("failed\n");
	exit(-1);
}

#---------------------------------------#

t = {}

count(t, "key")

if (t["key"] != 1) {
	failed()
}

count(t, "key")

if (t["key"] != 2) {
	failed()
}
